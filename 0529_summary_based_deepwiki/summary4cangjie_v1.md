# Summary Output - Kit Repository

> 由大模型 mimo-v2.5-pro 自动生成。
> 生成时间: 2026-05-29 15:28:35

---

# module `ohos/bundle/bundle.cj`

## function:

该模块提供 0 个类和 0 个函数，用于 ohos/bundle/bundle.cj 的核心功能实现。

## usage example:

```// 导入并使用 ohos/bundle/bundle.cj
```


# module `ohos/bundle/bundle_manager/ability_info.cj`

## function:

该模块提供 2 个类和 0 个函数，用于 ohos/bundle/bundle_manager/ability_info.cj 的核心功能实现。

## usage example:

```// 使用 AbilityInfo
auto obj = new AbilityInfo();
obj.someMethod();
```


# class `AbilityInfo`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new AbilityInfo();
```


# class `WindowSize`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new WindowSize();
```


# module `ohos/bundle/bundle_manager/application_info.cj`

## function:

该模块提供 3 个类和 0 个函数，用于 ohos/bundle/bundle_manager/application_info.cj 的核心功能实现。

## usage example:

```// 使用 ApplicationInfo
auto obj = new ApplicationInfo();
obj.someMethod();
```


# class `ApplicationInfo`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new ApplicationInfo();
```


# class `ModuleMetadata`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new ModuleMetadata();
```


# class `MultiAppMode`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new MultiAppMode();
```


# module `ohos/bundle/bundle_manager/bundle_flag.cj`

## function:

该模块定义了用于查询应用包信息的标志常量，通过组合这些标志可以精确

## usage example:

```// 使用 BundleFlag
auto obj = new BundleFlag();
obj.someMethod();
```


# class `BundleFlag`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new BundleFlag();
```


# module `ohos/bundle/bundle_manager/bundle_info.cj`

## function:

该模块定义了应用包（Bundle）的元数据结构，用于封装应用的基本信息、版本、依赖权限及内部模块配置，是包管理框架的核心数据模型。

## usage example:

```// 使用 BundleInfo
auto obj = new BundleInfo();
obj.someMethod();
```


# class `BundleInfo`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new BundleInfo();
```


# class `ReqPermissionDetail`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new ReqPermissionDetail();
```


# class `UsedScene`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new UsedScene();
```


# class `SignatureInfo`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new SignatureInfo();
```


# module `ohos/bundle/bundle_manager/bundle_manager.cj`

## function:

该模块提供 1 个类和 10 个函数，用于 ohos/bundle/bundle_manager/bundle_manager.cj 的核心功能实现。

## usage example:

```// 使用 BundleManager
auto obj = new BundleManager();
obj.someMethod();
```


# class `BundleManager`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new BundleManager();
```


# module `ohos/bundle/bundle_manager/cj_bundle_enum.cj`

## function:

该模块提供 0 个类和 7 个函数，用于 ohos/bundle/bundle_manager/cj_bundle_enum.cj 的核心功能实现。

## usage example:

```// 调用 parse
auto result = parse();
```


# module `ohos/bundle/bundle_manager/cj_bundle_ffi.cj`

## function:

该模块提供 0 个类和 17 个函数，用于 ohos/bundle/bundle_manager/cj_bundle_ffi.cj 的核心功能实现。

## usage example:

```// 调用 free
auto result = free();
```


# module `ohos/bundle/bundle_manager/cj_bundle_utils.cj`

## function:

该模块提供 0 个类和 12 个函数，用于 ohos/bundle/bundle_manager/cj_bundle_utils.cj 的核心功能实现。

## usage example:

```// 调用 readArrHapModuleInfo
auto result = readArrHapModuleInfo();
```



# module `ohos/bundle/bundle_manager/default_app_manager.cj`

## function:

该模块提供 1 个类和 1 个函数，用于 ohos/bundle/bundle_manager/default_app_manager.cj 的核心功能实现。

## usage example:

```// 使用 DefaultAppManager
auto obj = new DefaultAppManager();
obj.someMethod();
```


# class `DefaultAppManager`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new DefaultAppManager();
```


# module `ohos/bundle/bundle_manager/error_code.cj`

## function:

定义包管理器错误码常量，用于统一标识安装、卸载等操作中出现的各类异常。

## usage example:

```// 导入并使用 ohos/bundle/bundle_manager/error_code.cj
```


# module `ohos/bundle/bundle_manager/error_message.cj`

## function:

该模块提供 0 个类和 2 个函数，用于 ohos/bundle/bundle_manager/error_message.cj 的核心功能实现。

## usage example:

```// 调用 getErrorMsg
auto result = getErrorMsg();
```


# module `ohos/bundle/bundle_manager/extension_ability_info.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 ohos/bundle/bundle_manager/extension_ability_info.cj 的核心功能实现。

## usage example:

```// 使用 ExtensionAbilityInfo
auto obj = new ExtensionAbilityInfo();
obj.someMethod();
```


# class `ExtensionAbilityInfo`

## function:

该类封装了扩展能力的元数据信息，作为数据容器为系统提供扩展能力的描述和访问接口。

## extends:

none

## usage example:

```auto obj = new ExtensionAbilityInfo();
```


# module `ohos/bundle/bundle_manager/hap_module_info.cj`

## function:

该模块提供 5 个类和 0 个函数，用于 ohos/bundle/bundle_manager/hap_module_info.cj 的核心功能实现。

## usage example:

```// 使用 HapModuleInfo
auto obj = new HapModuleInfo();
obj.someMethod();
```


# class `HapModuleInfo`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new HapModuleInfo();
```


# class `DataItem`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new DataItem();
```


# class `Dependency`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Dependency();
```


# class `PreloadItem`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new PreloadItem();
```


# class `RouterItem`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new RouterItem();
```


# module `ohos/element_name/element_name.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 ohos/element_name/element_name.cj 的核心功能实现。

## usage example:

```// 使用 ElementName
auto obj = new ElementName();
obj.someMethod();
```


# class `ElementName`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new ElementName();
```


# module `ohos/metadata/metadata.cj`

## function:

该模块定义了OpenHarmony系统中的元数据实体，用于封装和管理应用或组件的键值对信息。它提供了元

## usage example:

```// 使用 Metadata
auto obj = new Metadata();
obj.someMethod();
```


# class `Metadata`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Metadata();
```


# module `ohos/skill/skill.cj`

## function:

该模块提供 2 个类和 0 个函数，用于 ohos/skill/skill.cj 的核心功能实现。

## usage example:

```// 使用 Skill
auto obj = new Skill();
obj.someMethod();
```


# class `Skill`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Skill();
```


# class `SkillUri`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new SkillUri();
```


# module `ohos/skill/skill_ffi.cj`

## function:

该模块提供 0 个类和 3 个函数，用于 ohos/skill/skill_ffi.cj 的核心功能实现。

## usage example:

```// 调用 free
auto result = free();
```



# module `ohos/zlib/cj_zlib.cj`

## function:

该模块提供 0 个类和 8 个函数，用于 ohos/zlib/cj_zlib.cj 的核心功能实现。

## usage example:

```// 调用 FfiBundleManagerCompressFile
auto result = FfiBundleManagerCompressFile();
```


# module `ohos/zlib/zip_error.cj`

## function:

该模块提供 0 个类和 2 个函数，用于 ohos/zlib/zip_error.cj 的核心功能实现。

## usage example:

```// 调用 getErrorMsg
auto result = getErrorMsg();
```


# module `ohos/zlib/zip_options.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 ohos/zlib/zip_options.cj 的核心功能实现。

## usage example:

```// 使用 Options
auto obj = new Options();
obj.someMethod();
```


# class `Options`

## function:

该类用于封装程序的配置选项数据，作为集中管理运行参数的容器，在

## extends:

none

## usage example:

```auto obj = new Options();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/Test_bundle_info.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 test/bundle_manager/test/entry/src/main/cangjie/Test_bundle_info.cj 的核心功能实现。

## usage example:

```// 使用 Test_bundle_info
auto obj = new Test_bundle_info();
obj.someMethod();
```


# class `Test_bundle_info`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Test_bundle_info();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/ability_stage.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 test/bundle_manager/test/entry/src/main/cangjie/ability_stage.cj 的核心功能实现。

## usage example:

```// 使用 MyAbilityStage
auto obj = new MyAbilityStage();
obj.someMethod();
```


# class `MyAbilityStage`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new MyAbilityStage();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/index.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 test/bundle_manager/test/entry/src/main/cangjie/index.cj 的核心功能实现。

## usage example:

```import test.bundle_manager.test.entry.*

main(): Int64 {
    let entryView = EntryView()
    entryView.show()
    return 0
}
```


# class `EntryView`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new EntryView();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/main_ability.cj`

## function:

该模块提供 1 个类和 0 个函数，用于 test/bundle_manager/test/entry/src/main/cangjie/main_ability.cj 的核心功能实现。

## usage example:

```// 使用 MainAbility
auto obj = new MainAbility();
obj.someMethod();
```


# class `MainAbility`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new MainAbility();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/test_list_bundle_manager.cj`

## function:

该模块负责注册和组织Bundle Manager相关的测试用例，通过调用测试套件注册函数来创建和执行针对bundle_info的测试。

## usage example:

```// 调用 testsuite
auto result = testsuite();
```


# module `test/bundle_manager/test/entry/src/main/cangjie/unittest_engine_pure_trunk.cj`

## function:

这是一个OpenHarmony应用的测试引擎模块，核心职责是提供UI自动化测试能力与测试用例管理

## usage example:

```// 使用 OpenHarmonyTestRunner
auto obj = new OpenHarmonyTestRunner();
obj.someMethod();
```


# class `OpenHarmonyTestRunner`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new OpenHarmonyTestRunner();
```


# class `UiComponentNotFoundById`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new UiComponentNotFoundById();
```


# class `UiComponentNotFoundByText`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new UiComponentNotFoundByText();
```


# class `AmbiguousUiComponentsFound`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new AmbiguousUiComponentsFound();
```


# class `Screen`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Screen();
```


# class `UiComponentTree`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new UiComponentTree();
```


# class `MyAction`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new MyAction();
```


# class `UiComponentById`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new UiComponentById();
```


# class `HybridUiComponent`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```component = HybridUiComponent()
component.render()
component.update()
component.destroy()
```


# class `Point`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Point();
```


# class `Event`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new Event();
```


# class `LogManager`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new LogManager();
```


# class `getInspector`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new getInspector();
```


# class `EventBuilder`

## function:

封装相关功能，提供 无 等方法。

## extends:

none

## usage example:

```auto obj = new EventBuilder();
```

